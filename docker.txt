mcr.microsoft.com/dotnet/aspnet:9.0 nema ARM64

M1 je igradjen na linux/arm64 docker bira platformu na kojoj je instaliran
zato radim --platform=linux/amd64 docker povuce amd64 image i pokrene ga preko emulacije




docker exec -it postgreDB psql -U postgres -d hmDB

TRUNCATE TABLE health_data RESTART IDENTITY CASCADE;


docker-compose up --build
docker-compose up -d
docker-compose build
docker-compose down [-v] // Brise value iz dockera



protoc --plugin=node_modules/.bin/protoc-gen-ts_proto --ts_proto_out=. -I. ./src/Protos/healthData.proto

docker exec -it mosquitto mosquitto_sub -h localhost -p 1883 -t "health/records"


dotnet ef database update
dotnet ef migrations add ime



npm -y init
npm install express
npm install --save-dev typescript ts-node @types/node @types/express nodemon
npx tsc --init
npm install dotenv
npm install --save-dev @types/dotenv



OpenAPI koriscen ja swagger

AsyncAPI koriscen je https://studio.asyncapi.com/#server-mosquitto za pisanje

